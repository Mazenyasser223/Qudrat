# Disable ESLint during build to avoid "Plugin react conflicted" when workspace has
# both root and client node_modules (eslint-config-react-app). Lint in editor/CI instead.
DISABLE_ESLINT_PLUGIN=true
