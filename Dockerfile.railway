# Railway-specific Dockerfile for server only
FROM node:18-alpine

WORKDIR /app

# Copy only server files
COPY server/package*.json ./
RUN npm install

# Copy server source code
COPY server/ ./

# Expose port
EXPOSE 5000

# Start the server
CMD ["npm", "start"]
